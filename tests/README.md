# Integration Tests

This directory contains integration tests that test multiple parts of the application working together.

## Structure

- `integration/` - Full application integration tests

## Running Tests

```bash
# Run all tests (unit + integration)
npm test

# Run only integration tests
npm test tests/

# Run a specific test file
npm test importArchiveWithAssets
```
